query authorities {
    authorities {
        id
    }
}

query validatorNodeProviders {
    validatorNodeProviders {
        id
        token {
            decimals
            id
            name
            symbol
        }
        payee
        price
    }
}
