query authorities {
    authorities {
        id
    }
}

query factories {
    factories {
        id
        price
        token {
            decimals
            id
            name
            symbol
        }
    }
}
