import { FC } from "react";
import Layout from "../components/layout";

const App: FC = () => {
    return (
        <Layout>
            <h1>Sunodo</h1>
        </Layout>
    );
};

export default App;
